name: aims_style_notify
description: WhatsApp-style Android CallStyle incoming-call notifications.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.aims.style_notify
        pluginClass: AimsStyleNotifyPlugin
